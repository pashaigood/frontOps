{
  "version": "independent",
  "packages": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "ignoreChanges": ["packages/cli/**", "packages/webpack/**"],
      "message": "chore(release): publish"
    },
    "bootstrap": {
      "private": false
    },
    "version": {
      "allowBranch": "master",
      "ignoreChanges": ["packages/cli/**", "packages/webpack/**"]
    }
  }
}
